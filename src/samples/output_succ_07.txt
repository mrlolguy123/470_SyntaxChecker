Success: no syntax error is found.

Following is the indentation-updated source code:
=================================================
func main::num()
begin
    var x::num;
    var y::num;
    x := x + 1;
    begin
        var x::bool;
        var y::void;
        x := x + 1;
        x := ( x + 1 ) * 2;
        x := x.size and y;
        x := x[ x + 1 ];
        x := new bool [ x.size ];
        x := new void [ x.size ];
    end
    x := ( x + 1 ) * 2;
end
